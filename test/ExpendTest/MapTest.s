
MapTest
{
    ArrClass
    {
        int i = 0;
    }
    ArrClass2
    {
        int i = 0;
    }
    static Fun()
    {       
        Map map = Map<string, string>(20);
        map.add( "xx", "20" );

        xx = "xx"

        Map map2 = Map<Class1, int>();
        Class1 c1 = Class1(20);
        map2.add(c1, 20);

        var mapv = map.$xx;  #这样是读取上边的变量  $变量  $"xx"  $c1 $0 
    }
}