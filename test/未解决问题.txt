函数名称和类名冲突时候的处理  

在linux wsl上跑起来，然后在ssh一台linux上跑下

编码相关的
Tcp/Udp/HttpClient/Websocket/NetStream相关接口
Yaml/Xml/RegExp


TankGame.sl 引入Math类。把一些 二维的都使用Int32_2 Int32_3

我现在要以plugins的形式 把F:\project\lang\SLPlugin\csharp_mono 接入进来，这个主要是用来跑csharp的源码的，我接入规则是这样的，在jsonc配置文件中，配置一个plugins的的字段，然后 里边{ "csharp_mono":{} } 来配置这个具体的过程 
"csharp_mono":
{
	"name":"csharp_runtime_use_mono_engine",
	"version": "2.0.1",
	"path":"csharp_mono",		//这个目录是插件的目录，需要加载lib下所有的库 因平台自动选择，如果选择导出的是android,则在lib/android/xxx.so的库  frontendLibs这个下边的是Frontend前端处理预加载的dll，可以是多个，主要是为了配合signLabel符号内代码解析的过程
	"signLabelHandle":{									//这个是否支持atSignLabel的用法，也就是@csharp(){} 的用法
		"name":"csharp",
		"params":[ {"version":"string","sync":true } ]	//这里边是 @csharp( 这里是参数的传入 version="8.0.0", sync = true ){},
	},
	"platform":{ "os":windows","cpu":"amd-x86" },
}

然后就是对 at sign label 的机制的判断  @gpu.amd 会在跑的平台， 在FrontEnd层要建一套 注册机制，在有这个标记的时候，会把当前使用的平台或者是机制信息，注册到刚才搭建的那个里边， 建一套，检查机制，只有遇到这个标记的才会跑，否则提示报错，不让代码跑起来，类推，如果我设计 的 带渲染的，但现在是在一套linux 不带图型的机器上，也跑不起来，只能通过运行屏蔽模块来限制这部分代码的运行，当然，也就不检查这部分的内容了。


使用 @csharp{ } 调用相关的接口 在{}内是中转逻辑   a = 0; @csharp {     a = TestCSharp.TTC.getIndex( 10, a ) }   @js{ a = Test.calc( a, a ) }  需要在工程里边配置 C#版本
我现在代码里边要增加 @标签{} 的一种逻辑，大概意思是，如果在jsonc 定义过这种标签，我在代码里边直接使用 比如  我在 jsonc 的配置里边  增加了 "atSignLabel":[ { "name":"asm", "handle":{}, "platform":{ "os":"window","cpu":"amd-x86" }  }, { "name":"csharp", "handle":{}, "platform":{ "os":"window","cpu":"amd-x86", "lib":"mono-static.dll" }  } ]  还有就是一些特殊注册，比如注册了csharp 可能识别代码 在@csharp(){ } 
13. 研究 -> <- 的符号的支持  这两符号为通道赋值的过程，也就是说，不是简单的赋值，而是检查当前的进程线程协程后，决定使用那种赋值的方式，如果需要中断的，或者是需要等待处理的，都要有相关的对应方法。

1. 接入 quickjs 
2. 接入python
3. 接入java

改二进制的大工程

官方第三方库:
libcurl
但做mysql数据库，需要动态引入dll的vm层代码 独立的工程
ffmpeg的dll 引入
找一个轻量级的 webview一类的，做成 dll


制定规则，在配置的jsonc里边，如果配置了，是否是public就可以为导出类  有三个选项 不导出，public,extern导出  

把global.data 确定了 下边的关联元素确定下，还有调用方式
把Core.module.json还有错误.csv 打包到.exe 然后 加载std需要另再加载

建一个统合工程，里边包括FrontEnd/Backend两部分，然后重整理cli然后把这两个都可以调用起来，并且做 编译即输出的调用。


1. cvm 只使用三色方案  然后使用 引用计数的方案，在结尾 后把内存收集  增加小块内存管理功能
机制差的:
优化FrontEnd层
增加tcp/udp/ip等协议 是否网络包，要独立 ，而不在std里边
再增加一些ErrorShow, netStream,
把静态绑定dll 参数尽量扩展下， 如果定义的export c的函数 是 比如char/char*/short/ushort/ptr* 等数型，都没有处理需要扩展，但貌似匹配机制你先看下还有没有更合理的机制 感觉现在要写的匹配太多了

1. this的使用，还没有整合
2. 循环引用 问题
6. 以cvm里边 数据类型的创建

1. 优化vm层，类型的处理   计算   比较  
混合类型运算
6.0x
while 循环迭代
6.3x
整数加法
6.4x
for 循环迭代
8.1x
bool 运算
8.2x
字符串比较
11.7x
函数调用
20.6x
字符串插值
37.5x
字符串拼接

内存管理，在cvm里边，增加一个机制，就是小块内存管理， 比如  byte32 byte40 byte 48  byte56  byte64  这样的，多个数组，然后以后用来存放像 Float16_2 的 Float32_2 以下的，都要有对应的列表，然后从这上边分配内存 
内存管理，增加引用计数，在函数体内检查与操作，如果在函数体几，引用的对应是本地创建的，在本函数内要释放  也就是函数结尾 要有对象的释放

在aot的中，处理llvm内置的一些结构，在生成这个数据后，要有一个id的概念，也就是定义这个对象的hashCode 虽然是在llvm里边生成的，也要有这个概念，因为这个在 llvm的.struct转成VMObject一类的时候，要先检查vm里边有没有这个，如果没有，再去创建VMObject 而且这个不受内存gc管理，因为这个还属于llvm  所以Object.sl 你看下是不是要增加一个int _hashCode这样的概念呀，是不是这样更合理  你确定下，原来好像我看着是用的指针进行hash运算的，这种方式确定可以吗？

在加载完CSharpModule后，先使用 调用nativeBridge的调用验证

sl_aot_skeleton_new_inner这个函数的话，原来的llvm生成的数据，是不是可以在转换的类中 member_data可以复用，然后 这个VMObject不进行gc拖管更合理。

优化：
1. 去掉int32/int64/...的一些初始化方法
2. 去掉无用的init
8. import 导入错误的显示
10. 全局的typealias里边需要读取后，继承
优化cvm里边的加法的过程

增加Jit的支持，起码实现x86的平台
增加对WebAssembly的支持，然后让网页上跑起来示例

MLIR把结构体，也就是我设计 的class/data/enum 要转化成对应的 llvm.struct   在变的过程，最好还能在这个结构里边带上一些其它的信息，比较类的id, 名称, 如果模板类，需要把模板的成员也变成一个指针（Int64)放在里边 最好还有一个字段去描述这个指针的，这样，不可以使用llvm.struct 去对应我的class/data/enum 了 
