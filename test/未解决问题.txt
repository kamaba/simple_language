1. 现在使用new的方式，需要重新走一遍 初始化成员，如果发现已有某个成员进行了初始化，则走自定义初始化成员的过程。
2. 使用MetaCallNode要生成相关的 生成类  还有函数，也要有自己存放的地址，以能判断后续的代码，是否正确
3. 整理，导入到IR层的东西，是否最精简的方式。
4. 开始测试多种模板情况及代码的正确性。
5. 普通的静态元素在做global的时候一起处理，存到一个固定区域，只有模型类的静态元素，是单独跟着模板创建时候走的.

3.1 注册类检查 参数 是否有子类与父类相同，甚至是模板类与普通函数相同的情况的优先级处理 比如 class1<T>{  add( object ob ){}  add( T t ){} } 这种情况，优化匹配add(object)
3. Type关键类的使用
4. New关键字，在VM中，初始化 相对应在函数  Class1 t = new(1,2,3){ t1 = 20}  其实 t1=20 
5. 优化虚拟机