
最近要解决的:
1. 基础类的相关写作 Object
2. 使用Num的完整
3. 使用Byte/SByte/Int16/UInt16/Int32/UInt32/
4. Float32/Float64
final int/uint/float 等方法 
toInt32...的方法使用
parseInt的尝试
compare的验证
int x1 = 10 + (1-20) * 33/20 + 10~/20 - 22 * 2/1; 有报错处理


如果是普通类，则看是否有重载，如果没有重载，则对比类引用是否是一个
2. 要优化new的时候初始化与assignMember两个有重复
3. 以cvm里边 数据类型的创建
4. enum 的赋值过程 
5. 统一赋值的过程的类型检查

1. 优化vm层，类型的处理
2. 在模板array 的 iteraror 的参数 argurment 需要 声明成templateObject
2.1. 统一创建runtimeClass的创建过程
2.3 需要创建 更多的系统类 Int32ToString/Int8ToString/
3. 需要导出类之间的关系的一个结构
4. 模板类的导出 需要把模板关联也导出
7. 调用nativeBridge的调用验证
6. 增加强制类型 float(object) 这类的强转处理
10. 指针的增加与处理

优化：
1. 去掉int32/int64/...的一些初始化方法
2. 去掉无用的init


1. c vm的使用
2. 内置action的支持
3. math库的处理
4. this的使用，还没有整合
5. base.也没有整合
6. 如果继续的extends Object 的toString() 没有override 需要报错
7. 增加基础库的一些函数
8. import 导入错误的显示


关于数字型计算，溢出的处理
1. 在Math里边提供 溢出环抱，溢出后最大值的处理 并且带参数确定是否要弹出异常收集
2. 在工程设置阶段，有全局的溢出处理方式，类中的溢子方式，方法的溢子方式  
现在要增加多线程编译，嗯，并且增加那个log里边儿取消编译的字段。如果当前的进程编译私有文件的时候出错了，那就取消当前进程编译。并且会影响到metal的编译方式啊，每个都这样做，如果metal中出错了，那就影响I2的输出。



以后要实现的:

1. 增加result关键字
2. 现在我要增加 ?符号的引入 ，其实代码里边已经 有一部分的处理了， 他的概念就是 如果定义一个类型，如果使用的是  type? 则这个结果是可以为空的，如果没有设置 则不可以为空    比如 int? 这个int就可以是空类型，如果没有带? 则走默认的值，所以在引入?前，要有一个概念 就是默认值概念，如果没有?的话，那必须这个要有默认值 ，包括 各种类，所以在类中，有一个默认值的概念后，主可以对?的处理了，如果定义过? 后，在传参，还有参考赋值的过程，就要考滤是否可为空的概念，如果不可为空，那在编译层就直接可以否定掉好多的逻辑，节省掉很多的空间 
3. bind的使用     iterface 接口 与bind的使用  如果使用interface的 如果bind过的话，那如果interface后，就相当于bind data了
4. bind在class上，后，就相当于  给class 添加一个 data DataName 然后把相关的方法进行了封闭  get DataNameVariableName{ DataName.variableName } set ...这种的
5. 增加协程  await 或者是使用go
6. 增加 catch的逻辑  数字计算的溢出，空指针等的收集 
6.1 . 每个溢出都，默认都会进入异常收集系统。
7 增加?? ?. ? 的语法糖 还有就是是否为空的空整确定
8. swtich的enum的支持  switch的,逗号的支持
9. switch的 区间支持 switch的is MetaClass 的支持
10. 写list,map的支持
11. 写json的支持 与data的联动
12. 快速返回 对=> 的支持