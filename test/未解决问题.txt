
最近要解决的:
1. 基础类的相关写作 Object
2. 使用Num的完整
3. 使用Byte/SByte/Int16/UInt16/Int32/UInt32/
4. Float32/Float64
final int/uint/float 等方法 
toInt32...的方法使用
parseInt的尝试
compare的验证
int x1 = 10 + (1-20) * 33/20 + 10~/20 - 22 * 2/1; 有报错处理


1. 优化vm层，类型的处理
2. 在模板array 的 iteraror 的参数 argurment 需要 声明成templateObject
2.1. 统一创建runtimeClass的创建过程
2.3 需要创建 更多的系统类 Int32ToString/Int8ToString/
3. 需要导出类之间的关系的一个结构
4. 模板类的导出 需要把模板关联也导出
7. 调用nativeBridge的调用验证
1. range->array 并且可以带缓存和dirty标记，如果重设置过某个值后，缓存清空
2. string.front(3) -> ByteArray 字符前3个字符
3. string.end(3)->byteArray 后边3个字符
4. 对 typealias ByteArray Array<Byte> 全局使用 的使用 IntArray的使用
5. string.range( 2, 4 ) ->byteArray 中间5个字符的使用
6. 

