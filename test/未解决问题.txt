
最近要解决的:
1. 基础类的相关写作 Object
2. 使用Num的完整
3. 使用Byte/SByte/Int16/UInt16/Int32/UInt32/
4. Float32/Float64
final int/uint/float 等方法 
toInt32...的方法使用
parseInt的尝试
compare的验证
int x1 = 10 + (1-20) * 33~/20 + 10~/20 - 22 * 2~/1; 有报错处理


1.1.  导出const 的数据，只允许在非template里边可以使用  然后在globalStaticVariableList 也要导出express里边，在读取的时候，把他们整合到前边的逻辑里边
1. 在运行类的时候或者创建类的时候，先要进行静态元素的初始化
2. 静态加载runtimeClass的，创建过程
2.1. 统一创建runtimeClass的创建过程
2.2. 整合staticMemberVariable的过程 
3. 需要导出类之间的关系的一个结构
4. 模板类的导出 需要把模板关联也导出
7. 调用nativeBridge的调用验证

可挑着做
