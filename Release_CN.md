# 极简语言 — 版本说明与路线图 [English](https://github.com/kamaba/simple_language/blob/main/Release.md)

本文描述 **语言与工具链的整体实现步骤**（路线图）、**当前快照版本 v0.0.001** 已具备能力，并与仓库根目录 **`target.md`** 中的长期设想对齐说明。详细零散条目仍以 `target.md` 原文为准，此处做结构化归纳，便于执行与排期。

---

## 一、整体设计：实现步骤（路线图）

下列阶段按**依赖顺序**排列；同一阶段内可并行推进子项。阶段编号不对应「第几期产品」，而是工程上的先后关系。

### 阶段 1 — 语言内核与类型系统

| 步骤 | 内容 |
|------|------|
| 1.1 | **基础框架与基础语法**：词法/语法管线、文件与命名空间组织、错误与日志基础设施。 |
| 1.2 | **控制流**：`if` / `elif` / `else`、`for` / `while`、`break` / `continue`、`switch`（含与表达式求值结合的用法）。 |
| 1.3 | **类型系统**：`as` / `is`；`string` 与整数、浮点等定宽数值类型（如 `int8`–`int64` 等）及一致性规则。 |
| 1.4 | **面向对象核心**：`namespace`、`class`、`data`、`enum`；`extends` 继承；`interface` 与实现关系。 |
| 1.5 | **序列与范围**：`array`、`range` 及与循环、索引的配合。 |
| 1.6 | **符号与运算符**：运算符重载（如 `_add_` / `_sub_` 等约定）；与**作用域**（`global` / `local` 等）规则统一语义，避免歧义与重名。 |

### 阶段 2 — 工程模型与 CLI

| 步骤 | 内容 |
|------|------|
| 2.1 | **工程即语言的一部分**：入口文件、编译列表、全局配置与源码根目录的一体化模型（当前以 `*.sp` + 同名 `*.jsonc` 为承载）。 |
| 2.2 | **CLI**：工程创建、向工程注册源文件、编译、导出 IR、运行与调试入口；与日志、`DebugCode` 产物联动。 |

### 阶段 3 — 语言增强关键字与元编程

| 步骤 | 内容 |
|------|------|
| 3.1 | **`bind` / `result` / `trycatch` / `attribute` 等**：在稳定内核之上扩展错误处理、结果类型、绑定与特性系统，并保证与 IR / VM 语义一致。 |

### 阶段 4 — 多后端与 Native 集成

| 步骤 | 内容 |
|------|------|
| 4.1 | **MLIR / LLVM 路径**：将部分函数体导出为 MLIR 或等价中间层，并能在 **VM 或运行时** 侧以 native 或预编译形式接入调用（与仅解释执行的路径分工）。 |
| 4.2 | **VM 自举**：将 VM 由 C# 迁移至 **C**（或 C 为核心），以降低运行时开销、简化与 native 及 FFI 的衔接。 |

### 阶段 5 — 容器与标准库

| 步骤 | 内容 |
|------|------|
| 5.1 | **Constraint 容器体系**：在类型与接口约束下提供 `list` / `set` / `map` / `queue` / `link` 等容器及统一迭代、性能策略。 |
| 5.2 | **标准库 `std`**：按模块推进 `io`、`net`、`os`、`sys`、`text`、`component`、`node`、`table`、`tree` 等，与工程 `references` / 模块图集成。 |

### 阶段 6 — 协议与异步

| 步骤 | 内容 |
|------|------|
| 6.1 | **协议系统**（interface / trait 式契约的演进形态）与 **`async` / `await`**：与调度器、VM 线程模型对齐后再定型。 |

### 阶段 7 — 编辑器与调试体验

| 步骤 | 内容 |
|------|------|
| 7.1 | **VSCode 扩展**：对接 CLI，支持编译、运行；后续迭代断点、栈、变量查看等调试能力。 |

### 阶段 8 — 数学类型、渲染与调试可视化

| 步骤 | 内容 |
|------|------|
| 8.1 | **常用数学类型**：`matrix`、`vector2` / `vector3`、`color` 等，尽量在运行时底层实现并配套 **`math` 库**。 |
| 8.2 | **Debug 渲染**：在 debug 模式下支持 `render` 等能力，通过 **OpenGL**（及后续可选 Vulkan/DX 等，见 `target.md`）输出到窗口或缓冲。 |

### 阶段 9 — FFI、并发与 UI 架构

| 步骤 | 内容 |
|------|------|
| 9.1 | **FFI**：封装对 **DLL** 等 native 库的调用，缩短与 C/C++ 生态的集成路径。 |
| 9.2 | **Isolate / 多线程**：进程内隔离与线程模型；**UI 渲染**与业务逻辑分离的模块化架构。 |

### 阶段 10 — 窗口、跨语言与数据可视化

| 步骤 | 内容 |
|------|------|
| 10.1 | **窗口系统**：创建窗口、与渲染管线结合。 |
| 10.2 | **跨语言库调用**：例如 **JavaScript / C# / Native** 的统一互操作层（与 `target.md` 中 `@js` / `@cs` / `@c` 等设想一致）。 |
| 10.3 | **表格与统计渲染**：读表（如表格文件）并在窗口中渲染统计图形。 |

### 阶段 11 — 科学计算、Web 与打包部署

| 步骤 | 内容 |
|------|------|
| 11.1 | **科学计算生态**：接入 **NumPy**、**Transformer** 及常见训练库等（以宿主进程或子进程 FFI 为现实路径，分阶段落地）。 |
| 11.2 | **Web 服务**：增强 API 抽象；**Swagger**；支持小型 Web 服务与**最小化打包部署**。 |

### 阶段 12 — IDE 与 AI 辅助

| 步骤 | 内容 |
|------|------|
| 12.1 | **Qt 开源 IDE**：安装、工程管理与调试一体化，侧重**科学计算与可视化**编辑体验。 |
| 12.2 | **AI 辅助代码生成**：在语言与库稳定后，基于规范语料与工具链训练模型，生成可编译、可测试的代码。 |

### 与 `target.md` 的关系

`target.md` 记录了更细颗粒的设想（例如 **CLR / JS / WASM** 多后端、**Markdown 融合注释**、**Profile / 反射 / 表格 Table**、**内嵌多语言片段**、**WebAssembly 在浏览器内编译** 等）。上表各阶段在排期时应**回溯 `target.md`**，将相关条目挂到对应阶段或拆成独立里程碑，避免重复造轮子或遗漏依赖。

---

## 二、当前版本：v0.0.001（2026-04-18）

**v0.0.001** 表示：仓库内已可跑通的主线能力快照（**非**语义化发行包版本号）。以下按「已具备 / 部分具备 / 未纳入本标签」说明。

### 2.1 已具备（主线可用）

- **编译前端（Front）与虚拟机（VM）**：C# 实现；源码 **`.sl`** 经多阶段解析生成 **IR**，导出为 **`*.module.json`** 供 VM 执行。  
- **工程系统**：**`*.sp` + 同名 `*.jsonc`** 管理源码根、编译文件列表、编译选项、`global.data` 等；入口 **`_main_` / `_test_`** 与 `Project{}`、`global` 的整合（见 `md/project/` 文档）。  
- **CLI**：工程创建、注册类文件、编译、导出 IR 等与配置联动（见 `md/project/project-config-jsonc-guide.md`）。  
- **类型与字面量**：多种整数/无符号/浮点、字符串、`boolean` 等；**`as` / `is`**；**`enum`**。  
- **控制流**：**`if` / `elif` / `else`**、**`for` / `while` / `do-while`**、**`switch`**、**`break` / `continue`**（含测试用例覆盖）。  
- **面向对象**：**`namespace`、`class`、继承 `extends`、接口 `interface`**；成员方法、静态成员、部分模板类用例。  
- **数组与范围**：**`array`**、**`range`** 相关能力与测试。  
- **运算符重载**：按语言约定的重载符号测试用例。  
- **作用域**：**`global` / `local`** 等与工程、元数据联动。  
- **关键字与特性（部分）**：**`bind`**；**`attribute`** 相关用例；**`try` / `catch`** 在解析与语言侧已有基础（完整语义与全部场景以测试与文档为准）。  
- **与 C# 的互操作**：绑定/替换等方向的测试与基础设施。  
- **调试与导出产物**：`Logs/`、`DebugCode/` 流水线及环境变量约定（见 `md/ai/DEBUG_WORKFLOW.md`、`EXPORT_PATHS.md`）。  
- **Core 库**：`samples/SLang/source` 下大量基础类型与核心类型的 **`.sl`** 实现，由 `Core.jsonc` 组织编译。

### 2.2 部分具备或实验性

- **MLIR**：仓库中存在 **导出/工具链相关代码与枚举**（如外部 `mlir-opt` 等调用路径），**不等同于**「函数级稳定导出 + VM 联调完成」的产品能力。  
- **容器 `constraint` 体系**：文档与部分测试（如 **List** 等）存在，**完整 `list/set/map/queue/link` 统一约束体系**仍在路线图中。  
- **协程等扩展测试**：`test/ExpendTest/` 中部分用例偏实验，**不默认视为稳定语言子集**。

### 2.3 本版本未纳入（见第一节路线图）

包括但不限于：**协议化 async/await 定型**、**完整 `std(io/net/…)` 标准库树**、**VSCode 正式扩展上架**、**OpenGL debug 渲染**、**FFI 正式封装**、**Isolate 多线程模型**、**VM 的 C 重写主线**、**矩阵/向量/color 一等公民类型**、**跨语言（JS 等）统一互操作**、**NumPy/训练栈接入**、**Qt IDE**、**Swagger Web 最小部署**、**自研 AI 代码生成** 等。

---

## 三、历史版本记录

### v0.0.5 [2022-12-23] 作者：kamaba

1. 前端语法解析与语言的逻辑整合。  
2. 暂不支持 array 与 template 相关。  
3. 提供简易的虚拟机。  
4. 提供转化中间语言。  
5. 支持基础运行。  
6. 支持基础类型：`byte`、`sbyte`、`char`、`short`、`ushort`、`int`、`uint`、`long`、`ulong`、`string`。

> 说明：与 **v0.0.001** 快照相比，仓库后续已演进（例如 array、模板类用例、jsonc 工程等）；上表仅作历史记录保留。
